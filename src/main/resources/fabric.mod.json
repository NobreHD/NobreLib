{
  "schemaVersion": 1,
  "id": "nobrelib",
  "version": "${version}",
  "name": "NobreLib",
  "description": "Just a library for some of my mods",
  "authors": [
    "NobreHD"
  ],
  "contact": {
    "repo": "https://github.com/nobrehd/NobreLib"
  },
  "license": "Apache-2.0",
  "icon": "assets/nobrelib/icon.png",
  "environment": "client",
  "entrypoints": {
    "client": [
      "pt.nobrehd.nobrelib.client.NobreLibClient"
    ],
    "main": [
      "pt.nobrehd.nobrelib.NobreLib"
    ]
  },
  "mixins": [
    "nobrelib.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}"
  }
}
